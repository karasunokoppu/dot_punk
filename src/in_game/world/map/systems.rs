use bevy::prelude::*;

use crate::in_game::world::map::components::{Map, Maps};

// pub fn load_map_data(
//     mut r_maps: ResMut<Maps>,
//     map: Map,
// ) {
//     r_maps.map_list.push(map);
// }
